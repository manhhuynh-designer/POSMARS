export interface NewTemplateConfig {
    title?: string
    bg_url?: string
    theme_color?: string
    // Add other fields here following snake_case naming convention
    // Ends URL fields with _url
}
